# _*_ encoding: utf-8 _*_

from argparse import ArgumentParser


# Arguments ==================================================================

args = ArgumentParser()

h_host = 'Host[:port]'

parser.add_argument(
                    '--host',
                    '-h',
                    help=h_host,
                    type=str,
                   )





# ============================================================================
